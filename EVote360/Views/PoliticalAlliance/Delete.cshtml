@model PoliticalAllianceDeleteViewModel 

@{
    ViewBag.Title = "title";
    Layout = "_LayoutAdministrator";
}

@{
    if (!string.IsNullOrEmpty(ViewBag.Message))
    {
        <div class="alert alert-info alert-dismissible fade show" role="alert">
            <p class="text-center">@Html.Raw(ViewBag.Message.Replace("\n", "<br />"))</p>
            <br/>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
}

<div class="row">
    <div class="col-12 col-md-4 offset-4">
        <div class="card">
            <div class="card-header text-center text-light bg-danger">
                <h5 class="card-title">
                    ¿Esta seguro de que desea eliminar esta solicitud de alianza?
                </h5>
            </div>
            <div class="card-body">
                <form asp-controller="PoliticalAlliance" asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" value="@Model.Id"/>
                    <div class="d-flex justify-content-center">
                        <button type="submit" class="btn btn-outline-danger me-2">Aceptar</button>
                        <a asp-controller="PoliticalAlliance" asp-action="Index" class="btn btn-outline-warning">Cancelar</a>
                    </div>
                </form>
            </div>

        </div>
    </div>
</div>
